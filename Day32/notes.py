#email smtp and datetime -> python inbuilt modules
#simple mail transfer protocol
import datetime as dt

now = dt.datetime.now()
year = now.year

#creating custom 
my_birthday = dt.datetime(year=1995, month=12,day=15)

#here index and data_row are the values that are being fetched in the iterations
birthdays_dict = {(data_row["month"], data_row["day"]): data_row for (index, data_row) in data.iterrows()}

#replacinng words in strings
txt = "i like you"

txt = txt.replace("like","hate")
print(x)
#this txt replacement return an output of the altered file, hence we need to change it by saving it again.

#deploying the project
#python anywhere
#upload all the files
#create bash console
#schedule the task to run everyday
