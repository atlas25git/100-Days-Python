#scopes
#no such thing as block scope in python
#only the stuff inside functions is treated as of local scope, rest others if/while have scopes that extend any blocks

if 3>2:
    var="Yes"

print(var)